<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatis.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.mybatis.entity.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="age" jdbcType="INTEGER" property="age" />
        <result column="sex" jdbcType="INTEGER" property="sex" />
    </resultMap>

    <select id="getUserById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select * from tb_user where id = #{id}
    </select>

    <insert id="userRegistry" parameterType="com.example.mybatis.entity.User">
        insert into tb_user(name, age, sex) values(#{name, jdbcType=VARCHAR}, #{age, jdbcType=INTEGER}, #{sex, jdbcType=INTEGER})
    </insert>

    <update id="updateUser" parameterType="com.example.mybatis.entity.User">
        update tb_user set
        <if test="name != null">
            name = #{name, jdbcType=VARCHAR},
        </if>
        <if test="age != null">
            age = #{age, jdbcType=INTEGER},
        </if>
        <if test="sex != null">
            sex = #{sex, jdbcType=INTEGER}
        </if>
        where id = #{id, jdbcType=INTEGER}
    </update>

    <update id="updateUserMap" parameterType="java.util.Map">
        update tb_user set
        <if test="name != null">
            name = #{name, jdbcType=VARCHAR},
        </if>
        <if test="age != null">
            age = #{age, jdbcType=INTEGER},
        </if>
        <if test="sex != null">
            sex = #{sex, jdbcType=INTEGER}
        </if>
        where id = #{id, jdbcType=INTEGER}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from tb_user where id = #{id, jdbcType=INTEGER}
    </delete>
</mapper>